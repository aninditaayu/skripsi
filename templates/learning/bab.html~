
{% extends 'learning/base.html' %}

{% load staticfiles %}

{% block title %}{{ nama }}{% endblock %}

{% block body_block %}
    <h1>{{ nama }}</h1>
{% if bab %}
            <ul>
                {% for b in bab %}
                <li>
                    <h2><a href="/learning/bab/{{ b.slug }}/">{{ b.nama }}</a></h2>
		<p> {{ b.deskripsi }} 	</p>
               </li> <!-- kasih href buat link-in bab-->
       <!--         {% endfor %}
            </ul>
        {% else %}
            <strong>Tidak ada bab yang tersedia.</strong>
        {% endif %}
{% endblock %}
